version: 1
frontend:
  phases:
    preBuild:
      commands:
        - gem install jekyll bundler
        - bundle install
    build:
      commands:
        - bundle exec jekyll build
  artifacts:
    baseDirectory: _site
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - vendor/**/*